{
  "name": "jam",
  "version": "1.0.0",
  "devDependencies": {
    "husky": "^6.0.0",
    "lint-staged": "^10.5.4",
    "prettier": "^2.2.1"
  },
  "scripts": {
    "prepare": "husky install"
  },
  "lint-staged": {
    "{pantry,ui}/**/*.{js,jsx}": "prettier --write --ignore-unknown"
  }
}
